const initialState = {
  charaters: [],
  isFeatching: false,
  errors: "",
};

export const reducer = (state = initialState, action) => {
  switch(action.type){
    default:
      return state;
  }
}